# 🏗️ Архитектура FOMO Voice Club

## Обзор системы

```
┌──────────────────────────────────────────────────────────────────┐
│                         КЛИЕНТЫ                                   │
├──────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐   │
│  │   Browser   │  │  Telegram   │  │    Mobile (future)      │   │
│  │  (React)    │  │   Clients   │  │                         │   │
│  └──────┬──────┘  └──────┬──────┘  └───────────┬─────────────┘   │
└─────────┼────────────────┼─────────────────────┼─────────────────┘
          │                │                     │
          ▼                ▼                     ▼
┌──────────────────────────────────────────────────────────────────┐
│                         API GATEWAY                               │
├──────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    FastAPI Server                          │  │
│  │                    (порт 8001)                             │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
          │                │                     │
          ▼                ▼                     ▼
┌──────────────────────────────────────────────────────────────────┐
│                         СЕРВИСЫ                                   │
├──────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ MongoDB  │  │ LiveKit  │  │ Telegram │  │    GridFS        │  │
│  │   (DB)   │  │  Cloud   │  │   Bot    │  │ (Audio Storage)  │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## Компоненты

### Frontend (React 19)

```
frontend/src/
├── App.js                    # Роутинг и провайдеры
├── pages/                    # Страницы приложения
│   ├── Home.jsx              # Главная страница
│   ├── LiveManagement.jsx    # Управление Live сессиями
│   ├── LiveRoomView.jsx      # Комната Live сессии
│   ├── MyProgress.jsx        # XP и прогресс пользователя
│   ├── AdminPanel.jsx        # Админ панель
│   ├── Library.jsx           # Библиотека подкастов
│   ├── PodcastDetail.jsx     # Детали подкаста
│   ├── TelegramSettings.jsx  # Настройки Telegram
│   └── Members.jsx           # Список участников
├── components/
│   ├── AudioPlayer.jsx       # Аудио плеер с WaveSurfer
│   ├── FloatingLivePlayer.jsx # Виджет свернутой сессии
│   ├── TelegramConnect.jsx   # Подключение Telegram
│   ├── Navigation.jsx        # Навигация
│   └── ui/                   # UI компоненты (Radix)
└── context/
    ├── AuthContext.jsx       # Аутентификация
    ├── WalletContext.jsx     # Wallet подключение
    └── LiveContext.jsx       # Состояние Live сессий
```

### Backend (FastAPI)

```
backend/
├── server.py                 # Главный файл приложения
├── models.py                 # Pydantic модели данных
├── routes/
│   ├── live_sessions.py      # Live сессии + LiveKit токены
│   ├── podcasts.py           # CRUD подкастов
│   ├── users.py              # Управление пользователями
│   ├── badges.py             # Система бейджей
│   ├── xp.py                 # XP система
│   ├── club.py               # Настройки клуба
│   ├── admin_panel.py        # Админ API
│   ├── telegram.py           # Telegram OAuth
│   ├── comments.py           # Комментарии
│   └── analytics.py          # Аналитика
├── services/
│   └── telegram_service.py   # Telegram отправка сообщений
├── telegram_bot.py           # Основной Telegram бот
└── telegram_recording_bot.py # Бот для записи сессий
```

---

## Потоки данных

### 1. Live Session Flow

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Админ     │────▶│  Создание   │────▶│   RTMP URL  │
│             │     │   сессии    │     │   генерация │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                               │
                    ┌─────────────┐            │
                    │   LiveKit   │◀───────────┘
                    │   Token     │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Спикер    │     │  WebSocket  │     │  Telegram   │
│  (микрофон) │     │   (чат)     │     │  (слушатели)│
└─────────────┘     └─────────────┘     └─────────────┘
```

### 2. XP Award Flow

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Действие   │────▶│  XP Award   │────▶│   Update    │
│ пользователя│     │   Service   │     │   User DB   │
└─────────────┘     └──────┬──────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │   Badge     │
                    │   Check     │
                    └──────┬──────┘
                           │
                           ▼
                    ┌─────────────┐
                    │  Auto-Award │
                    │   Badge     │
                    └─────────────┘
```

---

## База данных (MongoDB)

### Collections

```javascript
// users - Пользователи
{
  "id": "owner-001",
  "name": "Club Owner",
  "wallet_address": "0x...",
  "role": "owner",           // owner, admin, speaker, listener
  "level": 5,
  "xp_total": 10000,
  "xp_breakdown": {
    "listening_time": 3000,
    "live_attendance": 2500,
    "hand_raises": 100,
    "speeches_given": 200,
    "support_received": 50
  },
  "badges": [...],
  "telegram_connected": true,
  "telegram_chat_id": "12345"
}

// podcasts - Подкасты
{
  "id": "uuid",
  "title": "Episode Title",
  "description": "...",
  "author_id": "owner-001",
  "audio_url": "/static/audio/file.mp3",
  "duration": 3600,
  "listens": 100,
  "likes": 25,
  "tags": ["tech", "crypto"]
}

// live_sessions - Live сессии
{
  "id": "uuid",
  "title": "Live Show",
  "status": "active",        // scheduled, active, ended
  "host_id": "owner-001",
  "rtmp_url": "rtmps://...",
  "stream_key": "...",
  "participants": [...],
  "started_at": "ISO date",
  "ended_at": null
}

// club_settings - Настройки клуба
{
  "club_name": "FOMO Voice Club",
  "club_owner_wallet": "0x...",
  "club_admin_wallets": ["0x..."],
  "max_members": 1000,
  "enable_hand_raise": true
}

// xp_transactions - История XP
{
  "user_id": "owner-001",
  "action": "live_attendance",
  "xp_earned": 50,
  "timestamp": "ISO date"
}
```

### Indexes

```javascript
// users
db.users.createIndex({"id": 1}, {unique: true})
db.users.createIndex({"wallet_address": 1})
db.users.createIndex({"role": 1})
db.users.createIndex({"xp_total": -1})

// podcasts
db.podcasts.createIndex({"id": 1}, {unique: true})
db.podcasts.createIndex({"author_id": 1})
db.podcasts.createIndex({"created_at": -1})

// live_sessions
db.live_sessions.createIndex({"id": 1}, {unique: true})
db.live_sessions.createIndex({"status": 1})
```

---

## Интеграции

### LiveKit

**Назначение:** WebRTC голосовые комнаты

**Конфигурация:**
```python
LIVEKIT_API_KEY = "APIWrnERKLL3FHq"
LIVEKIT_API_SECRET = "lyFL5ewBGB2FK2iOizIjenTYeM9lgBAXHIAlEA99eNBC"
LIVEKIT_URL = "wss://podcast-4hfb3nr6.livekit.cloud"
```

**Использование:**
```python
from livekit import api

token = api.AccessToken(LIVEKIT_API_KEY, LIVEKIT_API_SECRET)
token.with_identity(user_id)
token.with_name(username)
token.with_grants(api.VideoGrants(
    room_join=True,
    room=session_id,
    can_publish=is_speaker,
    can_subscribe=True
))
jwt_token = token.to_jwt()
```

### Telegram

**Боты:**
- `@Podcast_FOMO_bot` - основной бот для уведомлений
- Recording Bot - мониторинг канала для записей

**Каналы:**
- `@P_FOMO` - уведомления
- `@Podcast_F` - записи сессий

---

## Безопасность

### Роли и права

| Роль | Права |
|------|-------|
| owner | Полный доступ, управление админами |
| admin | Создание контента, модерация |
| speaker | Выступление на сессиях |
| listener | Прослушивание, комментарии |

### Аутентификация

1. **Wallet-based:** Подключение MetaMask/WalletConnect
2. **Telegram OAuth:** Через Login Widget
3. **JWT токены:** Для API запросов

---

## Масштабирование

### Текущие лимиты

- LiveKit Free: 1-2 участника в комнате
- MongoDB: без ограничений
- Telegram: 20 сообщений/сек

### Рекомендации для продакшена

1. **LiveKit:** Перейти на платный план для большего количества участников
2. **MongoDB:** Использовать MongoDB Atlas
3. **CDN:** Cloudflare для статики и аудио
4. **Redis:** Для кеширования сессий
